apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-blob-storage
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: azureblob-nfs-premium
  resources:
    requests:
      storage: 5Gi