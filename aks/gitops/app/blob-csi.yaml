apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-blob-storage
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: azureblob-nfs-premium-compatible
  resources:
    requests:
      storage: 5Gi