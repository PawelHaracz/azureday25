apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-blob-storage-claim
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: azureblob-nfs-premium-retain
  resources:
    requests:
      storage: 5Gi